
### Десериализация котов

НЕОБХОДИМО: подключенные библиотеки из лекции https://javarush.ru/quests/lectures/jru.module2.lecture31

В метод getFromXml первым параметром приходит xml-строка, которая содержит один XML объект.
Вторым параметром приходит имя класса, объект которого находится в строке.
Метод getFromXml должен преобразовать объект из XML и вернуть его.

Используй метод readValue с параметрами (File, Class) или (Reader, Class).


Требования:
1.	В методе getFromXml должен быть создан объект типа XmlMapper с помощью конструктора без параметров.
2.	Объект возвращаемый методом getFromXml должен быть получен с помощью метода readValue(String, Class) класса ObjectMapper.


